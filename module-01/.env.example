# Weather API Key
# Used by both the agent (client) and weather service (server)
WEATHER_API_KEY=test_api_key_12345

# Weather API URL
# Points to the local weather service
WEATHER_API_URL=http://localhost:8000/data/2.5/weather
